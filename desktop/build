#!/usr/bin/env bash
set -euo pipefail

DESKTOP_ROOT="$(cd "$(dirname "$0")" && pwd)"
KIWI_ROOT="$(cd "$DESKTOP_ROOT/.." && pwd)"
export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-$KIWI_ROOT/target}"
export RUSTFLAGS="${RUSTFLAGS:--D warnings}"

exec "$KIWI_ROOT/build" "$@"
