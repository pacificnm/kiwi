name = "kiwi-agent-claude"
version = "0.1.0"
min_kiwi_version = "0.1.0"
display_name = "Claude"
description = "Anthropic Claude Code — runs as the AI agent in the Agent panel."
author = "PacificNM"

[agent]
command = "claude"
args = []

[install]
kind = "copy"
files = ["plugin.toml"]
